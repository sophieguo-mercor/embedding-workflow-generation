# ── Data (large, may contain PII) — never commit ─────────────────────────────
# Covers the raw exports AND the cleaned twins under data/cleaned/ (clean.py).
data/**/*.xlsx
data/**/*.csv
data/**/*.xls
data/*.xlsx
data/*.csv
data/*.xls

# ── Secrets ──────────────────────────────────────────────────────────────────
.env
*.env
.envrc

# ── Embedding & LLM caches (bulky, regenerable) ──────────────────────────────
cache/
*.npz
*.parquet

# ── Pipeline outputs (may echo raw ticket text / pre-PII-gate content) ───────
# Regenerated by the pipeline (run.py / finalize.py mkdir these dirs).
results/

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.DS_Store

# Excel lock files
~$*.xlsx
