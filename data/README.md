# Data files (gitignored)

The pipeline expects two Power BI exports here:

- `Tickets Mercor v3.xlsx`
- `Time entries Mercor v3.xlsx`

These are large and may contain PII, so they are **not committed** (see `.gitignore`).
Copy them in from the shared `ticket_classification_dashboard/sample_data` source.
