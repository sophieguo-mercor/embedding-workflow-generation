pandas>=2.0
openpyxl>=3.1
scikit-learn>=1.3
numpy>=1.24
openai>=1.40.0
tiktoken>=0.7.0        # accurate token counts for embedding batch sizing (optional; falls back to a heuristic)
anthropic>=0.40.0
umap-learn>=0.5.5      # ENT-2289: UMAP dimensionality reduction before HDBSCAN
hdbscan>=0.8.33        # ENT-2289: density clustering (sklearn.cluster.HDBSCAN is the fallback)
